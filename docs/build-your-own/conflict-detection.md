# Conflict Detection

!!! note "Draft"
    Add a detector by subclassing `ConflictDetector` and implementing
    `detect(own, intr, rpz, t_lookahead) -> bool`. See the built-in `StateBased`. To be written.
