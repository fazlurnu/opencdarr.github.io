# Recovery Criteria

!!! note "Draft"
    Add a recovery rule by subclassing `RecoveryCriterion` and implementing
    `should_resume(own, intr, rpz) -> bool`. See `PastCPA`, `FTR`, and `ProbabilisticFTR`. To be written.
