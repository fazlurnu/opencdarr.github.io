# Autopilot

!!! note "Draft"
    Add guidance by subclassing `Autopilot` and implementing `step(state, memory, perf)`, which returns
    the nominal `MotionCommand` and the updated `GuidanceMemory`. See `CruiseAutopilot` and
    `WaypointAutopilot`. To be written.
