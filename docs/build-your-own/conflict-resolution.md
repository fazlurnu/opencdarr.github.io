# Conflict Resolution

!!! note "Draft"
    Add a resolver by subclassing `ConflictResolver` and implementing
    `resolve(own, intruders, rpz, preferred) -> MotionCommand`. See `MVP` and `VO`. To be written.
