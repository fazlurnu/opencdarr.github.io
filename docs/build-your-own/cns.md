# CNS

!!! note "Draft"
    Model your own communication, navigation, and surveillance by subclassing `NavigationModel`
    (`measure`), `CommunicationModel` (`step`), or `SurveillanceModel` (`perceived`). See
    `GnssNavigation`, `Comm`, and `LastKnown`. To be written.
