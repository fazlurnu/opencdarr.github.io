# Kinematics

!!! note "Draft"
    Add a vehicle by subclassing `Kinematics` and implementing `step(state, command, perf, dt, wind)` —
    a pure map from one state to the next. See the built-in `Multirotor` and `FixedWing`. To be written.
